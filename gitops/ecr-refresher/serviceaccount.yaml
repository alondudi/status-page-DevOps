apiVersion: v1
kind: ServiceAccount
metadata:
  name: ecr-refresher
  namespace: status-page
  annotations:
    # Uses the node instance profile (AmazonEC2ContainerRegistryReadOnly already attached)
    # No IRSA needed — node role grants ECR read access to all pods on the node
